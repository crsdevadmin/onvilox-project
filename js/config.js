// CONFIGURATION: Centralized API URLs
const CONFIG = {
  // During local development, use localhost:3000
  // After deploying to Render or AWS, update this URL to your new cloud endpoint
  // Example: "https://onvilox-api.onrender.com"
  API_BASE_URL: "http://localhost:3000"
};

console.log("ONVILOX_CONFIG_LOADED");
