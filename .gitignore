# Environment Variables (SENSITIVE)
.env
server/.env

# Dependencies
node_modules/
server/node_modules/

# OS Files
.DS_Store
Thumbs.db
.elasticbeanstalk/app_versions/

# FUSE artefacts — created when a mounted file is replaced while still open.
# Not project files; they appear as untracked junk and must never be committed.
.fuse_hidden*
**/.fuse_hidden*
